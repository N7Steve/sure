<%# locals: (payment:, size: "lg") %>
<% if payment.transfer? %>
  <%= render DS::FilledIcon.new(icon: "arrow-right-left", hex_color: Category::TRANSFER_COLOR, size: size, rounded: true,
        description: t("scheduled_payments.form.payment_types.transfer")) %>
<% elsif (merchant_logo_url = payment.merchant&.display_logo_url(family: Current.family)).present? %>
  <%= image_tag merchant_logo_url,
        class: class_names("rounded-full shrink-0 object-cover", size.to_s == "sm" ? "w-6 h-6" : "w-9 h-9"),
        loading: "lazy", alt: "" %>
<% else %>
  <%= render DS::FilledIcon.new(variant: :text, text: payment.merchant&.name || payment.title, size: size, rounded: true) %>
<% end %>
